name: oefening 3
on:
  workflow_dispatch: {}
  push:
    branches:
      - main

jobs:
  get_data:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@master
      - name: update diagram
        uses: githubocto/repo-visualizer@main
        with:
          should_push: false
